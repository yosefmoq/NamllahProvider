<resources>
    <!-- Base application theme. -->
    <style name="Theme.NamllahProvider" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/primary_color_dark</item>
        <item name="colorAccent">@color/accent_color</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>

    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
        <item name="boxStrokeColor">@color/primary_color</item>
        <item name="hintTextColor">@color/primary_color</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_bottom_sheet</item>
    </style>


    <style name="AppButton" />

    <style name="AppButton.AccentButton">
        <item name="android:background">@drawable/selector_accent_bg</item>
        <item name="android:textColor">@drawable/selector_accent_color</item>
    </style>

    <style name="AppButton.Primary">
        <item name="android:background">@drawable/selector_primary_bg</item>
        <item name="android:textColor">@drawable/selector_primary_color</item>
    </style>
</resources>